get:
  operationId: getTeacherCourses
  tags:
    - teacherSecure
  parameters:
    - $ref: '../../components/parameters/PageableParams.yaml#/parameters/OffsetParam'
    - $ref: '../../components/parameters/PageableParams.yaml#/parameters/CountParam'
  responses:
    '200':
      description: Страница курсов админа.
      content:
        application/json:
          schema:
            $ref: '../../components/schemas/teacher-secure/response/GetCoursesOfAdminResponseDto.yaml'

post:
  operationId: createCourse
  tags:
    - teacherSecure
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: '../../components/schemas/teacher-secure/request/CreateCourseRequestDto.yaml'
  responses:
    '200':
      description: Возвращает созданный курс.
      content:
        application/json:
          schema:
            $ref: '../../components/schemas/teacher-secure/response/CreateCourseResponseDto.yaml'

