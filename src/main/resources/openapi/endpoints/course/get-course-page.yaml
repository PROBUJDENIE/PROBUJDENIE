get:
  security: []
  operationId: getCoursePage
  tags:
    - course
  parameters:
    - name: offset
      in: query
      required: true
      schema:
        type: integer
        format: int32
    - name: count
      in: query
      required: true
      schema:
        type: integer
        format: int32
  responses:
    '200':
      description: Страница актуальных курсов
      content:
        application/json:
          schema:
            $ref: '../../components/course/response/CourseGetCoursePageResponseDto.yaml'
