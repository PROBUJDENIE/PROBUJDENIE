get:
  operationId: getSectionPage
  tags:
    - public
  parameters:
    - $ref: '../../components/parameters/PageableParams.yaml#/parameters/OffsetParam'
    - $ref: '../../components/parameters/PageableParams.yaml#/parameters/CountParam'
    - name: courseId
      in: path
      required: true
      schema:
        type: string
        format: UUID
  responses:
    '200':
      description: Список секций курса
      content:
        application/json:
          schema:
            $ref: '../../components/schemas/public/response/GetSectionPageResponseDto.yaml'