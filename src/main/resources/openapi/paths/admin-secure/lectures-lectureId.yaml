delete:
  operationId: deleteLecture
  tags:
    - adminSecure
  parameters:
    - name: lectureId
      in: path
      required: true
      schema:
        type: string
        format: UUID
  responses:
    '200':
      description: Возвращает 'success'
      content:
        application/json:
          schema:
            $ref: '../../components/responses/BaseResponseDto.yaml'

put:
  operationId: updateLecture
  tags:
    - adminSecure
  parameters:
    - name: lectureId
      in: path
      required: true
      schema:
        type: string
        format: UUID
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: '../../components/schemas/admin-secure/request/UpdateLectureRequestDtoAdmin.yaml'
  responses:
    '200':
      description: Возвращает 'success'
      content:
        application/json:
          schema:
            $ref: '../../components/responses/BaseResponseDto.yaml'
