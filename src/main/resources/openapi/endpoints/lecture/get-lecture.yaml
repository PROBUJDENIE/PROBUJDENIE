get:
  operationId: getLecture
  tags:
    - lecture
  parameters:
    - name: id
      in: path
      required: true
      schema:
        type: string
        format: UUID
  responses:
    '200':
      description: Возвращается лекция
      content:
        application/json:
          schema:
            $ref: '../../components/lecture/response/LectureGetLectureResponseDto.yaml'
