get:
  operationId: getSectionPage
  tags:
    - section
  parameters:
    - name: offset
      in: query
      required: true
      schema:
        type: integer
        format: int32
        minimum: 0
    - name: count
      in: query
      required: true
      schema:
        type: integer
        format: int32
        minimum: 0
    - name: courseId
      in: path
      required: true
      schema:
        type: string
        format: UUID
  responses:
    '200':
      description: Список секций курса
      content:
        application/json:
          schema:
            $ref: '../../components/section/response/SectionGetSectionPageResponseDto.yaml'