get:
  operationId: getCoursePage
  tags:
    - studentSecure
  parameters:
    - name: offset
      in: query
      required: true
      schema:
        type: integer
        format: int32
    - name: count
      in: query
      required: true
      schema:
        type: integer
        format: int32
  responses:
    '200':
      description: Возвращает список курсов.
      content:
        application/json:
          schema:
              $ref: '../response/GetUserCoursesResponseDto.yaml'