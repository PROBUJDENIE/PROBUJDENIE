allOf:
  - $ref: '../../../responses/BaseResponseDto.yaml'
  - type: object
    properties:
      data:
        type: object
        title: CreateLectureResponseDtoItem
        required:
          - id
          - title
          - orderNumber
        properties:
          id:
            type: string
            format: UUID
          title:
            type: string
            minLength: 3
            maxLength: 255
          contentId:
            type: string
            format: UUID
          orderNumber:
            type: integer
            format: int32
            minimum: 0

