post:
  operationId: createLecture
  tags:
    - teacherSecure
  parameters:
    - name: sectionId
      in: path
      required: true
      schema:
        type: string
        format: UUID
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: '../../components/schemas/teacher-secure/request/CreateLectureRequestDto.yaml'
  responses:
    '200':
      description: Возвращает лекцию
      content:
        application/json:
          schema:
            $ref: '../../components/schemas/teacher-secure/response/CreateLectureResponseDto.yaml'
