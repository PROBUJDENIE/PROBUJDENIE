post:
  operationId: buyCourse
  tags:
    - studentSecure
  parameters:
    - name: courseId
      in: path
      required: true
      schema:
        type: string
        format: UUID
  responses:
    '200':
      description: Покупка курса.
      content:
        application/json:
          schema:
              $ref: '../../components/BaseResponseDto.yaml'