post:
  operationId: createSection
  tags:
    - teacherSecure
  parameters:
    - name: courseId
      in: path
      required: true
      schema:
        type: string
        format: UUID
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: '../../components/schemas/teacher-secure/request/CreateSectionRequestDto.yaml'
  responses:
    '200':
      description: Возвращает секцию
      content:
        application/json:
          schema:
            $ref: '../../components/schemas/teacher-secure/response/CreateSectionResponseDto.yaml'