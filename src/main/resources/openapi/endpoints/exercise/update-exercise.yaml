put:
  operationId: updateExercise
  tags:
    - exercise
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: '../../components/exercise/request/UpdateExerciseRequest.yaml'
  responses:
    '200':
      description: Возвращает 'success'
      content:
        application/json:
          schema:
            $ref: '../../components/BaseResponseDto.yaml'