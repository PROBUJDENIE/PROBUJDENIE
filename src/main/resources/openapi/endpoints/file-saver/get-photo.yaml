get:
  operationId: getPhoto
  tags:
    - fileSaver
  requestBody:
    required: true
    content:
        multipart/form-data:
          schema:
            $ref: '../../components/file-saver/CoursePhoto/GetCoursePhotoRequestDto.yaml'
  responses:
    '200':
      description: Возвращает photo
      content:
        image/png:
          schema:
            type: string
            format: binary