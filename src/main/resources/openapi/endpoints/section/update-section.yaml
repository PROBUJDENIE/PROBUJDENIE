put:
  operationId: updateSection
  tags:
    - section
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: '../../components/section/request/SectionUpdateRequestDto.yaml'
  responses:
    '200':
      description: Возвращает 'success'
      content:
        application/json:
          schema:
            $ref: '../../components/BaseResponseDto.yaml'