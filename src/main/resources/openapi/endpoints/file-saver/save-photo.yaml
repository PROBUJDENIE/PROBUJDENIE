post:
  operationId: savePhoto
  tags:
    - fileSaver
  requestBody:
    required: true
    content:
        multipart/form-data:
          schema:
            $ref: '../../components/file-saver/CoursePhoto/SaveCoursePhotoRequestDto.yaml'
  responses:
    '200':
      description: Возвращает photoId
      content:
        application/json:
          schema:
            $ref: '../../components/file-saver/CoursePhoto/SaveCoursePhotoResponseDto.yaml'