post:
  operationId: save
  tags:
    - fileSaver
  requestBody:
    required: true
    content:
        multipart/form-data:
          schema:
            $ref: '../../components/file-saver/SaveFileSaverRequestDto.yaml'
  responses:
    '200':
      description: Возвращает id объекта в S3
      content:
        application/json:
          schema:
            $ref: '../../components/file-saver/SaveFileSaverResponseDto.yaml'