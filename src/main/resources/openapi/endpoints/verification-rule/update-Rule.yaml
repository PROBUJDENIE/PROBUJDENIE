put:
  operationId: updateVerificationRule
  description: Обновляет информацию о правиле задания
  tags:
    - verificationRule
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: '../../components/verification-rule/request/UpdateVerificationRuleRequest.yaml'
  responses:
    '200':
      description: Возвращает 'success'
      content:
        application/json:
          schema:
            $ref: '../../components/BaseResponseDto.yaml'